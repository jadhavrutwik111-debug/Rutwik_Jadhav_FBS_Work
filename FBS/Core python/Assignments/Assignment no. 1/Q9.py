#calculte area of triangle
#take inputs for base and height
b = int(input('Enter the base of triangle: '))
h = int(input('Enter the height of triangle: '))

#perform claculation
area = 0.5 * b * h

#display result 
print('The area of triangle is:', area)